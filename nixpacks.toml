[phases.setup]
nixPkgs = [
  "python3",
  "gcc",
  "glib",
  "libx11",
  "libxcb",
  "libxext",
  "libsm",
  "libice",
  "libglvnd"
]

[phases.install]
cmds = [
  "pip install -r requirements.txt"
]

[start]
cmd = "uvicorn main:app --host 0.0.0.0 --port $PORT"