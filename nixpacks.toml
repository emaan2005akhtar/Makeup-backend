[phases.setup]
nixPkgs = [
  "python3",
  "gcc",
  "libglvnd",
  "glib",
  "libx11",
  "libxcb",
  "libxext",
  "libsm",
  "libice"
]

[phases.install]
cmds = [
  "pip install -r requirements.txt"
]

[start]
cmd = "uvicorn main:app --host 0.0.0.0 --port $PORT"