[phases.setup]
aptPkgs = [
  "libgl1",
  "libglib2.0-0",
  "libsm6",
  "libxext6",
  "libxrender1",
  "libx11-6",
  "libxcb1",
  "libgtk2.0-0",
  "libgtk-3-0"
]

[phases.install]
cmds = [
  "pip install -r requirements.txt"
]

[start]
cmd = "uvicorn main:app --host 0.0.0.0 --port $PORT"